<div class="js--mobile-feature">
	{% set featuredInHome = craft.entries.section('blog').featuredInHomepage('1').limit(5).find() %}
{% paginate craft.entries.section('blog').limit(5).order('postDate desc') as blogEntries %}
{% for featuredContent in featuredInHome %}
		<div class="feature-item">
			<div class="media">
				{% set image = featuredContent.featuredImage.first() %}
				{% if image %}
		          <figure><img src="{{ image.getUrl('squareh') }}" alt="{{ entry.title }}"/></figure>
		          {% endif %}

			</div>
			<div class="content">
			    <h2>{{ featuredContent.title }}</h2>
			    <div class="desc">{{featuredContent.subHeading}}</div>
			    {% if featuredContent.type == 'link' %}
			    <a href="" class="btn-link">{{ featuredContent.linkName }}</a>
			    {% elseif featuredContent.type == 'video' %} 
			    <a href="{{ featuredContent.url }}" class="btn-link" title="{{ featuredContent.title }}">ver ahora</a> 
			    {% else %}
			    <a href="{{ featuredContent.url }}" class="btn-link" title="{{ featuredContent.title }}">leer más</a>
			    {% endif %}
			  </div>
		</div>
		
{% endfor %}

 {% endpaginate %}
</div>